# Wiki Lint — 2026-05-04

## Health Summary

The wiki infrastructure remains entirely absent one week after it was first flagged — no wiki skill, no content pages, no knowledge base, and no log; only the `lint/` folder (containing last week's report) exists under `groups/global/wiki/`.

## Findings (priority order)

**[HIGH]** `container/skills/wiki/SKILL.md` — Wiki container skill file is still missing. The lint procedure cannot be loaded from the canonical source; this lint is performed from prompt context only. Flagged in the 2026-04-27 report with no action taken. — **Fix:** Install the wiki skill (run `/add-karpathy-llm-wiki`) so `SKILL.md` is created before the next lint cycle.

**[HIGH]** `groups/global/wiki/index.md` — File does not exist. Without an index the wiki has no entry point, no entity registry, and no inbound-link anchor for orphan-page checks. — **Fix:** Install the wiki skill; the setup step creates `index.md` and `log.md` automatically.

**[HIGH]** `groups/global/wiki/log.md` — File does not exist. Log-hygiene check cannot be performed. — **Fix:** Same as above — created as part of wiki skill setup.

**[HIGH]** `groups/global/knowledge/` — Directory still does not exist. No source material has been ingested. Flagged in the 2026-04-27 report with no action taken. — **Fix:** After installing the wiki skill, run a first ingest to establish folder structure.

**[LOW]** `groups/global/wiki/lint/2026-04-27.md` — Prior lint report exists but none of its three HIGH findings have been resolved after one week. Not a structural issue, but worth noting for prioritisation. — **Fix:** No file change needed; action on the HIGH items above will clear this implicitly.

## Clean

All content-level checks were **skipped** — no content files exist to evaluate:

- Contradictions
- Stale claims
- Orphan pages
- Missing pages
- Missing cross-references
- Data gaps (beyond the global gap already flagged above)

## Recommended next sources

1. **Wiki skill installation** — Installing `/add-karpathy-llm-wiki` is the single highest-leverage action; it unblocks every other check and recommendation.
2. **Personal/project seed document** — Once the skill is installed, ingesting a foundational reference (personal profile, project overview, or a frequently-referenced document) gives the agent something to build the first real pages from.
3. **log.md bootstrap verification** — After the first ingest, confirm `wiki/log.md` exists and its opening entry carries a valid `ingest:` op prefix; this makes the log-hygiene check meaningful in the next cycle.
