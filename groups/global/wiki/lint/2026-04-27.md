# Wiki Lint — 2026-04-27

## Health Summary

The wiki and knowledge base do not exist yet — neither `groups/global/wiki/` nor `groups/global/knowledge/` have been created, and `container/skills/wiki/SKILL.md` is absent, indicating the wiki skill has not been installed.

## Findings (priority order)

**[HIGH]** `groups/global/wiki/` — Directory does not exist. The wiki skill (`/add-karpathy-llm-wiki` or equivalent) has not been installed on this instance. No `index.md`, `log.md`, or any page files are present. — **Fix:** Install the wiki skill and run an initial ingest to create the structure before the next lint cycle.

**[HIGH]** `groups/global/knowledge/` — Directory does not exist. No knowledge source files have been ingested. — **Fix:** After installing the wiki skill, run the first knowledge ingest so folder structure is established.

**[HIGH]** `container/skills/wiki/SKILL.md` — The wiki container skill file is missing; the lint procedure itself could not be loaded from the canonical source. This lint was performed from the prompt context only. — **Fix:** Merge the wiki skill branch so the SKILL.md is present for future automated lints.

## Clean

All content-level checks (contradictions, stale claims, orphan pages, missing pages, missing cross-references, data gaps, log hygiene) were **skipped** — no content files exist to check.

## Recommended next sources

1. **Wiki skill installation** — The highest-value action is installing the wiki skill so the infrastructure exists; without it no other sourcing is meaningful.
2. **Initial knowledge seed** — Once installed, seed `groups/global/knowledge/` with a foundational source document (e.g., a personal profile, project overview, or recurring reference doc) so the first ingest has material to work with.
3. **log.md bootstrap** — After the first ingest, verify `wiki/log.md` exists and contains a properly prefixed `ingest` entry; this will make the log-hygiene check meaningful in the next lint cycle.
