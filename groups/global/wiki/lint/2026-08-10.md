# Wiki Lint — 2026-08-10

## Health Summary

For the fourteenth consecutive week the wiki infrastructure is entirely absent — no wiki skill, no content pages, no knowledge base, and no log; the `lint/` folder (now containing fourteen prior reports) remains the only content under `groups/global/wiki/`.

## Findings (priority order)

**[HIGH]** `container/skills/wiki/SKILL.md` — Wiki container skill file is still missing. Lint cannot be loaded from the canonical source; this report is performed from prompt context only. First flagged 2026-04-27, unfixed through fourteen lint cycles. — **Fix:** Install the wiki skill (run `/add-karpathy-llm-wiki`) so `SKILL.md` is created before the next lint cycle.

**[HIGH]** `groups/global/wiki/index.md` — File does not exist. Without an index the wiki has no entry point, no entity registry, and no anchor for orphan-page or missing-page checks. First flagged 2026-04-27, unfixed through fourteen lint cycles. — **Fix:** Install the wiki skill; setup creates `index.md` and `log.md` automatically.

**[HIGH]** `groups/global/wiki/log.md` — File does not exist. Log-hygiene check cannot be performed. First flagged 2026-04-27, unfixed through fourteen lint cycles. — **Fix:** Same as above — created as part of wiki skill setup.

**[HIGH]** `groups/global/knowledge/` — Directory does not exist. No source material has been ingested. First flagged 2026-04-27, unfixed through fourteen lint cycles. — **Fix:** After installing the wiki skill, run a first ingest to establish folder structure.

**[MEDIUM]** `groups/global/wiki/lint/` — Fourteen weekly lint reports (2026-04-27 through 2026-08-10) have accumulated, all flagging identical HIGH issues with no resolution. The scheduled lint is running correctly; the actionable fixes are being consistently deferred. — **Fix:** No file change needed; resolving the HIGH items above will clear this implicitly.

## Clean

All content-level checks were **skipped** — no content files exist to evaluate:

- Contradictions
- Stale claims
- Orphan pages
- Missing pages
- Missing cross-references
- Data gaps (beyond the global infrastructure gap flagged above)
- Log hygiene
- Knowledge completeness

## Recommended next sources

1. **Wiki skill installation** — Fourteen cycles of blocked checks make this the single highest-leverage action. Running `/add-karpathy-llm-wiki` creates `index.md`, `log.md`, and `container/skills/wiki/SKILL.md` in one step, unlocking all subsequent checks.
2. **Personal/project seed document** — Once the skill is installed, ingesting a foundational reference (personal profile, project overview, or a frequently-referenced document) gives the agent concrete material for the first real pages.
3. **log.md bootstrap verification** — After the first ingest, confirm `wiki/log.md` exists and its opening entry carries a valid `ingest:` op prefix so the log-hygiene check becomes meaningful in the next cycle.
